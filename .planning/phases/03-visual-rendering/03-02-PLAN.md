---
phase: 03-visual-rendering
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [FLOW-01, FLOW-08]
must_haves:
  truths: ["Flowchart button in GapStrike opens the visual editor with styled boxes and arrows", "Each box displays cloze syntax raw with accent highlight", "Preview toggle switches between editor and raw HTML render"]
  artifacts: []
  key_links: []
---

<objective>
Human-verify that the FlowchartEditor renders correctly in the running app with real AI-generated flowchart HTML.

Purpose: Automated tests confirm data flow and compilation, but visual rendering (box styling, arrow alignment, branch layout, dark theme) requires human eyes. This checkpoint confirms FLOW-01 (visual rendering) and FLOW-08 (cloze display) in the actual app.

Output: Verified working visual editor or issue list for gap closure.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-visual-rendering/03-CONTEXT.md
@.planning/phases/03-visual-rendering/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify build</name>
  <files>gapstrike/src/components/FlowchartEditor.tsx</files>
  <action>
1. Run `cd gapstrike && npm run dev` to start the Next.js dev server
2. Run `cd gapstrike && npx vitest run` to confirm all tests pass (existing + smoke)
3. Run `cd gapstrike && npx tsc --noEmit` to confirm no TypeScript errors
4. If any errors, fix them before proceeding to checkpoint
  </action>
  <verify>
    <automated>cd gapstrike && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Dev server running, all tests pass, no TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify visual rendering of FlowchartEditor</name>
  <files>gapstrike/src/components/FlowchartEditor.tsx</files>
  <action>Present the running app to the user for visual verification of FLOW-01 and FLOW-08.</action>
  <verify>Human confirms visual correctness</verify>
  <done>User approves or provides issue list</done>
  <what-built>FlowchartEditor visual rendering with dark-themed boxes, arrows, step pills, cloze highlighting, and Preview toggle</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in the browser
    2. Navigate to the Anki card creation flow and trigger a Flowchart generation (or use an existing flowchart card)
    3. Verify the visual editor appears with:
       a. Dark dot-grid background on the editor canvas
       b. Boxes rendered as rounded dark cards with border
       c. Cloze markers like {{c1::Thiamine deficiency}} visible as raw text with purple accent highlight
       d. Step label pills appearing as small italic badges between boxes
       e. Vertical stems (thin lines) connecting boxes
       f. If a branching flowchart: horizontal fan-out with L-shaped corner connectors
       g. Hover over boxes: subtle lift effect with shadow
    4. Click the "Preview in Anki" toggle button in the header bar
       - View should switch to raw HTML render (the same output Anki sees)
       - Click "Back to Editor" to return to the interactive view
    5. Verify the flowchart title appears in the header bar
    6. Check that no light-theme colors appear (no white backgrounds, no stone palette)
  </how-to-verify>
  <resume-signal>Type "approved" if the visual editor looks correct, or describe any issues (alignment, colors, missing elements)</resume-signal>
</task>

</tasks>

<verification>
Human confirms visual rendering matches requirements FLOW-01 and FLOW-08.
</verification>

<success_criteria>
- Human confirms boxes, arrows, pills, and cloze highlights render correctly in the dark theme
- Preview toggle works bidirectionally
- No visual regressions in FlowView.tsx (FlowchartPreview still works)
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-rendering/03-02-SUMMARY.md`
</output>
