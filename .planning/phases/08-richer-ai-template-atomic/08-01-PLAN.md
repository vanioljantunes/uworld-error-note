---
phase: 08-richer-ai-template-atomic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gapstrike/src/lib/template-defaults.ts
  - gapstrike/tests/flow-round-trip.test.ts
  - gapstrike/tests/template-hash.test.ts
autonomous: true
requirements:
  - TMPL-07

must_haves:
  truths:
    - "TEMPLATE_PREV_HASHES contains the old hash c9d31786fcdb0678 so uncustomized user templates auto-upgrade"
    - "The anki_flowchart template instructs GPT-4o to produce 5-7 node flowcharts with labeled causal arrows and educational-objective-driven cloze"
    - "parseFlowHTML correctly parses 5+ node linear and branching HTML structures without triggering textarea fallback"
    - "Category-hint cloze syntax survives parse and rebuild round-trip"
  artifacts:
    - path: "gapstrike/src/lib/template-defaults.ts"
      provides: "Rewritten anki_flowchart template + updated TEMPLATE_PREV_HASHES"
      contains: "c9d31786fcdb0678"
    - path: "gapstrike/tests/flow-round-trip.test.ts"
      provides: "Richer-structure parse fixtures (5-node linear, 6-node branching, category-hint cloze)"
      min_lines: 180
    - path: "gapstrike/tests/template-hash.test.ts"
      provides: "Hash bookkeeping test — old hash in TEMPLATE_PREV_HASHES, new hash differs"
      min_lines: 10
  key_links:
    - from: "gapstrike/src/lib/template-defaults.ts"
      to: "gapstrike/src/app/api/templates/route.ts"
      via: "TEMPLATE_PREV_HASHES import for auto-upgrade logic"
      pattern: "TEMPLATE_PREV_HASHES"
    - from: "gapstrike/tests/flow-round-trip.test.ts"
      to: "gapstrike/src/lib/parse-flow-html.ts"
      via: "parseFlowHTML import — fixtures validate parser handles richer HTML"
      pattern: "parseFlowHTML"
---

<objective>
Atomically rewrite the anki_flowchart template for richer 5-7 node mechanism maps, update TEMPLATE_PREV_HASHES with the old hash, and add parse/round-trip test fixtures for the new structure.

Purpose: This is the core of Phase 8 — the template prompt drives GPT-4o to generate richer flowcharts with educational-objective-driven cloze, and the hash update ensures uncustomized users auto-upgrade. Tests prove the parser handles the new structures.
Output: Updated template-defaults.ts (template + hash), new test fixtures in flow-round-trip.test.ts, new template-hash.test.ts
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-richer-ai-template-atomic/08-CONTEXT.md
@.planning/phases/08-richer-ai-template-atomic/08-RESEARCH.md

@gapstrike/src/lib/template-defaults.ts
@gapstrike/src/lib/parse-flow-html.ts
@gapstrike/src/lib/rebuild-flow-html.ts
@gapstrike/tests/flow-round-trip.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From gapstrike/src/lib/template-defaults.ts:
```typescript
export interface TemplateDefault {
  slug: string;
  category: "error_note" | "anki";
  title: string;
  content: string;
}

export const TEMPLATE_PREV_HASHES: Record<string, string[]> = {
  anki_flowchart: ["d2343b1e21aa9df1", "a5f7aade1b01b248", "195d2fc7a40117fd", "6c7928647efcdecb", "ab29f95e3c05a983", "607faa7057d4a280"],
  anki_table: ["a3b9de9e219c4927"],
};
```

From gapstrike/src/lib/parse-flow-html.ts (role detection — DO NOT change these style strings):
```
title: font-weight:bold AND font-size:14px
branch: display:inline-flex (checked before box)
box: display:inline-block AND border:2px solid #3a3a3a
pill: display:inline-block AND font-size:10px
stemWrap: single child with width:2px AND background:#3a3a3a
```

From gapstrike/tests/flow-round-trip.test.ts (existing fixture pattern):
```typescript
// FIXTURE_LINEAR: 4-box chain with cloze
// FIXTURE_BRANCHING: 1 root branching into 2 children
// Tests use parseFlowHTML() and rebuildHTML() imports
```

Hash computation (from api/templates/route.ts):
```typescript
function contentHash(content: string): string {
  return createHash("sha256").update(content).digest("hex").slice(0, 16);
}
// Current anki_flowchart hash: c9d31786fcdb0678
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add richer-structure test fixtures and hash bookkeeping test</name>
  <files>gapstrike/tests/flow-round-trip.test.ts, gapstrike/tests/template-hash.test.ts</files>
  <behavior>
    - Test: parseFlowHTML on a 5-box linear chain returns exactly 5 nodes, 4 edges, 0 branchGroups
    - Test: parseFlowHTML on a 6-box branching structure (4 linear + 2-arm branch) returns >= 5 nodes, >= 1 branchGroup
    - Test: Category-hint cloze (e.g. cN::Hormone-sensitive lipase::enzyme) survives parse, and survives parse + rebuild round-trip
    - Test: TEMPLATE_PREV_HASHES["anki_flowchart"] includes "c9d31786fcdb0678"
    - Test: Current anki_flowchart template content hashes to something OTHER than "c9d31786fcdb0678" (proves template was actually changed — this test will fail RED until Task 2 rewrites the template)
  </behavior>
  <action>
    1. In `gapstrike/tests/flow-round-trip.test.ts`, add three new HTML fixtures after the existing FIXTURE_BRANCHING:

       **FIXTURE_RICHER_LINEAR**: A 5-box DKA-style linear chain. Build by hand using the EXACT same style strings as FIXTURE_LINEAR (copy-paste the style attributes). Structure:
       - Title: "DKA Mechanism"
       - Box: "Insulin deficiency" (trigger, no cloze)
       - Pill: "activates" -> Stem
       - Box: "{{c1::Hormone-sensitive lipase::enzyme}}" (distinguishing step, clozed with category hint)
       - Pill: "releases" -> Stem
       - Box: "Free fatty acid oxidation" (intermediate)
       - Pill: "disrupts" -> Stem
       - Box: "Ketone accumulation" (intermediate)
       - Pill: "depletes" -> Stem
       - Box: "Anion gap metabolic acidosis" (leaf outcome, no cloze)
       Use verb labels from the allowed vocabulary (activates, releases, disrupts, depletes).

       **FIXTURE_RICHER_BRANCHING**: A 6-box structure — 4 boxes in linear chain, then branch into 2 leaf outcomes (6 nodes total). Structure:
       - Title: "ACE Inhibitor Mechanism"
       - Box: "ACE inhibitor administered"
       - Pill: "blocks" -> Stem
       - Box: "{{c1::Angiotensin-converting enzyme::target}}"
       - Pill: "depletes" -> Stem
       - Box: "Angiotensin II levels decrease"
       - Stem -> Branch (display:inline-flex) with 2 arms:
         - Left arm: pill "decreases" -> Box: "Aldosterone secretion reduced"
         - Right arm: pill "accumulates" -> Box: "{{c2::Bradykinin::mediator}} buildup"
       Use the EXACT same style strings from FIXTURE_BRANCHING for the branch structure (copy the border-top, border-left, border-right patterns).

       **FIXTURE_CATEGORY_HINT**: Can reuse FIXTURE_RICHER_LINEAR (it already has category-hint cloze). No separate fixture needed — just add a test that checks the round-trip.

    2. Add a new `describe` block: `'parseFlowHTML — richer 5+ node structures (TMPL-07)'` with tests:
       - 'parses a 5-box linear chain returning 5 nodes and 4 edges'
       - 'parses a 6-box branching structure with >= 5 nodes and >= 1 branchGroup'
       - 'category-hint cloze survives parse'
       - 'category-hint cloze survives parse + rebuild round-trip'

    3. Create `gapstrike/tests/template-hash.test.ts`:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { createHash } from 'crypto';
       import { TEMPLATE_DEFAULTS, TEMPLATE_PREV_HASHES } from '../src/lib/template-defaults';

       describe('TEMPLATE_PREV_HASHES bookkeeping (TMPL-07)', () => {
         it('anki_flowchart prev hashes include c9d31786fcdb0678', () => {
           expect(TEMPLATE_PREV_HASHES['anki_flowchart']).toContain('c9d31786fcdb0678');
         });

         it('current anki_flowchart content hash differs from all prev hashes', () => {
           const tpl = TEMPLATE_DEFAULTS.find(t => t.slug === 'anki_flowchart');
           const hash = createHash('sha256').update(tpl!.content).digest('hex').slice(0, 16);
           expect(TEMPLATE_PREV_HASHES['anki_flowchart']).not.toContain(hash);
         });
       });
       ```

    Note: The second hash test will fail RED until Task 2 changes the template content. This is expected TDD behavior.
  </action>
  <verify>
    <automated>cd gapstrike && npx vitest run tests/flow-round-trip.test.ts</automated>
  </verify>
  <done>All richer-structure parse tests pass (5-node linear, 6-node branching, category-hint round-trip). template-hash.test.ts exists with 2 tests (first passes after Task 2 adds the hash; second passes after Task 2 changes the template).</done>
</task>

<task type="auto">
  <name>Task 2: Atomic rewrite of anki_flowchart template + TEMPLATE_PREV_HASHES update</name>
  <files>gapstrike/src/lib/template-defaults.ts</files>
  <action>
    Edit `gapstrike/src/lib/template-defaults.ts` to make TWO changes in a single edit:

    **Change 1: Add old hash to TEMPLATE_PREV_HASHES**
    Append `"c9d31786fcdb0678"` to the `anki_flowchart` array (it becomes the 7th entry).

    **Change 2: Rewrite the anki_flowchart template content**
    Find the `TEMPLATE_DEFAULTS` entry with `slug: "anki_flowchart"` and rewrite its `content` string. The new template must preserve the four section markers (`<!-- section: System Prompt -->`, `<!-- section: Instructions -->`, `<!-- section: Card Structure -->`, `<!-- section: Rules -->`).

    **System Prompt section** changes:
    - Keep the existing color palette, layout rules, and role description
    - Add to the arrow-label instruction: expand from the current ~5 verbs to the full curated vocabulary across 3 domains:
      - Pharmacology: binds to, blocks, agonizes, antagonizes, upregulates, downregulates, sensitizes, potentiates, inhibits
      - Pathophysiology: damages, inflames, disrupts, occludes, compresses, infiltrates, necroses, fibroses, depletes, activates
      - Anatomy/Clinical: innervates, drains to, supplies, crosses, presents as, metastasizes to, refers to
    - Keep the "NEVER use generic 'leads to', 'causes', 'then'" rule

    **Instructions section** changes (Phase 1 analysis expansion):
    - Keep existing questions 1-5
    - Add question 6: "What is the EDUCATIONAL OBJECTIVE? What specific fact separates the correct answer from the most tempting wrong alternative? This is the DISTINGUISHING STEP."
    - Add question 7: "What are the WRONG ALTERNATIVES? At which node in the mechanism do students diverge to pick the wrong answer? That divergence node is the strongest cloze candidate."
    - These are thinking-only instructions (do NOT include in output) — keep them inside the existing Phase 1 block

    **Card Structure section** changes:
    - Replace BOTH existing examples (Wernicke + Kidney embryology) with:

    **New Example 1: DKA Mechanism (pathophysiology, branching)**
    - Title: "DKA Mechanism"
    - Educational objective commentary in Phase 1 thinking: "DKA vs HHS — the distinguishing step is ketone production via lipolysis activation. In HHS, insulin is low but present enough to suppress lipolysis. If a student forgets that hormone-sensitive lipase activation is the key divergence, they pick HHS."
    - Chain: Insulin deficiency (trigger, no cloze) -> activates -> (c1::Hormone-sensitive lipase::enzyme) (clozed — this is the distinguishing step) -> releases -> Free fatty acid oxidation -> produces -> Ketone body accumulation -> Branch into: [presents as -> Kussmaul breathing] [depletes -> Bicarbonate buffer]
    - That gives 7 boxes (trigger + 3 intermediate + 1 branch node + 2 leaf) with branching
    - Use EXACT style strings from the existing examples (same box, pill, stem, branch patterns — copy them character-for-character)

    **New Example 2: ACE Inhibitor Mechanism (pharmacology, branching)**
    - Title: "ACE Inhibitor Mechanism"
    - Educational objective commentary: "ACE inhibitor vs ARB — the distinguishing step is bradykinin accumulation. ARBs block AT1 receptors downstream, so they don't affect bradykinin. If a student forgets that ACE also degrades bradykinin, they can't explain the dry cough side effect."
    - Chain: ACE inhibitor (e.g., lisinopril) (trigger) -> blocks -> (c1::Angiotensin-converting enzyme::target) (clozed) -> depletes -> Angiotensin II -> Branch into: [decreases -> (c2::Aldosterone secretion::hormone)] [accumulates -> Bradykinin]
    - That gives 6 boxes with branching
    - EXACT same style strings

    CRITICAL STYLE STRING RULES (from research — parser will break if violated):
    - Box: `border:2px solid #3a3a3a;padding:8px 16px;display:inline-block;background:#1a1a1a;color:#e2e2e2;border-radius:4px` — NO spaces after colons
    - Pill: `display:inline-block;padding:2px 10px;font-size:10px;color:#777;font-style:italic;border:1px solid #2a2a2a;border-radius:8px;background:#111`
    - Stem: `width:2px;height:15px;background:#3a3a3a;margin:0 auto` (12px height in branch arms)
    - Title: `font-size:14px;font-weight:bold;margin-bottom:10px;color:#e2e2e2`
    - Branch container: `display:inline-flex`
    - Branch arm left: `height:15px;border-top:2px solid #3a3a3a;border-left:2px solid #3a3a3a;margin-left:50%`
    - Branch arm right: `height:15px;border-top:2px solid #3a3a3a;border-right:2px solid #3a3a3a;margin-right:50%`

    NOTE: In the actual template examples, use proper Anki cloze syntax with double curly braces. The notation (cN::text::hint) in this plan is shorthand to avoid YAML parsing issues — the real code must use the standard Anki cloze format.

    **Rules section** changes:
    - Change minimum box count from 4 to 5: "The diagram MUST contain 5-7 boxes."
    - Add rule: "Cloze the DISTINGUISHING STEP identified in Phase 1 analysis. If forgetting this step would cause a student to pick the most tempting wrong alternative, it must be clozed. Triggers and leaf outcomes are rarely clozed unless the trigger/outcome itself IS the distinguishing fact."
    - Add rule: "Use category hints for clozed terms (e.g. c1::Thiamine::vitamin in Anki cloze format). NEVER use wrong-alternative text as a hint."
    - Add rule: "Arrow labels MUST come from the domain vocabulary listed in the System Prompt. NEVER use: leads to, causes, then, results in."
    - Per user's locked decision (CONTEXT.md), only "leads to", "causes", and "then" are explicitly banned. "produces" is allowed and used in the DKA example.

    After BOTH changes, verify:
    - `TEMPLATE_PREV_HASHES["anki_flowchart"]` has 7 entries (last is `c9d31786fcdb0678`)
    - The template content is changed (different hash)
    - All style strings in examples match the parser detection patterns exactly

    Then run ALL tests to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd gapstrike && npx vitest run</automated>
  </verify>
  <done>template-defaults.ts contains: (1) TEMPLATE_PREV_HASHES with 7 entries including c9d31786fcdb0678, (2) rewritten anki_flowchart template with expanded Phase 1 analysis (educational objective + wrong alternatives), expanded arrow vocabulary (22 domain verbs across 3 categories), 2 new branching examples (DKA + ACE inhibitor), rules requiring 5-7 nodes and distinguishing-step cloze with category hints. All vitest tests pass including the new hash and richer-structure tests.</done>
</task>

</tasks>

<verification>
1. `cd gapstrike && npx vitest run` — all tests pass (existing + new richer-structure + hash bookkeeping)
2. `TEMPLATE_PREV_HASHES["anki_flowchart"]` has exactly 7 entries
3. The new template content's hash is NOT in TEMPLATE_PREV_HASHES (it's a new hash)
4. New template examples use ONLY the exact style strings the parser expects (no whitespace variations)
5. Template has both DKA and ACE inhibitor examples with branching structures
</verification>

<success_criteria>
- All existing tests still pass (no regressions)
- 4+ new tests pass (5-node linear, 6-node branching, category-hint round-trip, hash bookkeeping)
- Template content has Phase 1 analysis questions 6-7 (educational objective, wrong alternatives)
- Template rules require 5-7 nodes with distinguishing-step cloze and category hints
- Template arrow vocabulary covers 22 domain-specific verbs across pharmacology/pathophysiology/anatomy
- TEMPLATE_PREV_HASHES includes c9d31786fcdb0678 (auto-upgrade works)
- All changes in template-defaults.ts committed atomically
</success_criteria>

<output>
After completion, create `.planning/phases/08-richer-ai-template-atomic/08-01-SUMMARY.md`
</output>
