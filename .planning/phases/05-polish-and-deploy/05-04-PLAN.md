---
phase: 05-polish-and-deploy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TMPL-06]
gap_closure: true

must_haves:
  truths:
    - "The production editor at gapstrike.vercel.app reflects the Phase 4/5 FlowchartEditor changes (editable node cards, toolbar, parse-failure fallback)"
    - "Generating a flowchart card on production opens the visual editor with boxes and arrows rendered"
  artifacts:
    - path: "gapstrike.vercel.app (production)"
      provides: "Live deployment with current FlowchartEditor build"
  key_links:
    - from: "git push origin master"
      to: "Vercel auto-deploy"
      via: "Vercel git integration"
      pattern: "gapstrike.vercel.app serves bundle matching commits b53981e + 3672e9e"
---

<objective>
Close verification gaps from 05-VERIFICATION.md: the production editor UI at gapstrike.vercel.app appears stale (Issue 1) and card preview does not render (Issue 2). Diagnose root cause (Vercel build cache, browser cache, or build error), fix, and re-verify the full generate/edit/save flow on production.

Purpose: The phase 5 success criterion "deployed Vercel app includes all flowchart and table editor changes with no regressions" failed. This plan fixes the deploy gap.
Output: Working production deployment confirmed via human smoke-test.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-deploy/05-03-SUMMARY.md
@.planning/phases/05-polish-and-deploy/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix stale production deployment</name>
  <files></files>
  <action>
Investigate why gapstrike.vercel.app shows a stale editor UI despite commits b53981e + 3672e9e being on remote master. Follow this diagnostic sequence:

1. **Check Vercel deployment status** — Run `npx vercel ls` or check `npx vercel inspect` to see the latest deployment and whether it completed successfully. If the Vercel CLI is not authenticated, create a checkpoint for the user to authenticate.

2. **Check if the build actually succeeded** — Run `npx vercel logs` on the latest deployment to look for build errors. A silent build failure would cause Vercel to serve the last successful build (stale).

3. **If build succeeded but site is stale** — This is likely a CDN/edge cache issue. Trigger a redeployment:
   - Run `npx vercel --prod --force` from the gapstrike/ directory to force a fresh production deploy
   - OR if CLI auth is an issue, make an empty commit and push: `git commit --allow-empty -m "chore: trigger Vercel redeploy" && git push origin master`

4. **If build failed** — Read the build logs to identify the error. Fix any TypeScript or Next.js build errors in the gapstrike/ codebase. Run `npm run build` locally in gapstrike/ to confirm the fix, then push.

5. **Verify the deployed bundle is fresh** — After redeployment, use `curl -sI https://gapstrike.vercel.app | grep -i 'x-vercel\|cache\|date'` to confirm the response headers show a recent deployment timestamp and no stale cache.

The goal is to get the production site serving the current master branch code. Issue 2 (no card preview) is expected to resolve automatically once Issue 1 (stale build) is fixed.
  </action>
  <verify>
    <automated>curl -sI https://gapstrike.vercel.app | grep -i "x-vercel-id\|date"</automated>
  </verify>
  <done>Vercel deployment logs show a successful build from the latest master commit. Response headers confirm a fresh deployment. The gapstrike.vercel.app site is serving the current codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production smoke-test — full generate/edit/save flow</name>
  <files></files>
  <action>
Human verifies the production deployment at gapstrike.vercel.app. This is a checkpoint — no automated action. The user visually confirms the editor UI is current and the full generate/edit/save flow works end-to-end.

What was built: Fresh Vercel deployment of gapstrike with all Phase 4/5 FlowchartEditor and TableEditor changes.

How to verify:
1. Open https://gapstrike.vercel.app in a fresh browser tab (hard-refresh with Ctrl+Shift+R to bypass browser cache)
2. Confirm the editor UI shows the updated Phase 4 layout: editable node cards, "Add Box" / "Connect" toolbar buttons, editor/preview toggle
3. Generate a flowchart card: select a note, click "Flowchart" button, wait for AI generation
4. Confirm the visual editor opens with boxes and arrows rendered (Issue 2 fix)
5. Edit a box label — confirm the change appears in the preview
6. Save the card to Anki — confirm AnkiConnect accepts the save (no errors)
7. Open the browser console (F12) — confirm no JavaScript errors related to the editor
8. (Optional) Generate a table card and verify the table editor also works

Expected: All steps pass. The full generate -> edit -> save flow works end-to-end on production.

Resume signal: Type "approved" if the full flow works, or describe any issues found.
  </action>
  <verify>Human visual confirmation of production smoke-test</verify>
  <done>Human approves that the production site shows the current FlowchartEditor UI and the full generate/edit/save flow works without errors.</done>
</task>

</tasks>

<verification>
- Production site at gapstrike.vercel.app serves the current master branch build
- FlowchartEditor visual editor renders correctly on production (Issue 1 resolved)
- Card preview renders inside editor panel on production (Issue 2 resolved)
- Full generate/edit/save flow confirmed working by human smoke-test
</verification>

<success_criteria>
- gapstrike.vercel.app shows the Phase 4/5 FlowchartEditor with editable node cards and toolbar
- Generating a flowchart card opens the visual editor with boxes and arrows
- A card can be edited and saved to Anki from the production site without errors
- Human approves the production smoke-test
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-deploy/05-04-SUMMARY.md`
</output>
