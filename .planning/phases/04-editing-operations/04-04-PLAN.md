---
phase: 04-editing-operations
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [INTG-01, INTG-02]

must_haves:
  truths:
    - "Clicking Flowchart button in GapStrike triggers AI generation and opens FlowchartEditor with working inline editing"
    - "Clicking Table button in GapStrike triggers AI generation and opens TableEditor with working cell editing"
    - "Edits in FlowchartEditor update the card FRONT field HTML visible in preview"
    - "Edits in TableEditor update the card FRONT field HTML visible in preview"
  artifacts: []
  key_links:
    - from: "FlowView.tsx handleSwitchEditor"
      to: "FlowchartEditor onChange"
      via: "setEditFront callback propagation"
      pattern: "setEditFront"
    - from: "FlowView.tsx handleSwitchEditor"
      to: "TableEditor onChange"
      via: "setEditFront callback propagation"
      pattern: "setEditFront"
---

<objective>
Human-verify the full end-to-end integration: Flowchart button triggers AI generation then opens FlowchartEditor with working editing; Table button triggers AI generation then opens TableEditor with working editing. Both editors propagate changes to the card FRONT field.

Purpose: INTG-01 and INTG-02 require visual/functional verification that the full flow (AI generation -> editor open -> edit -> FRONT field update) works in the browser. This cannot be automated.
Output: Verified integration or list of issues to fix.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-editing-operations/04-01-SUMMARY.md
@.planning/phases/04-editing-operations/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm everything is green before human verification:
    ```bash
    cd gapstrike && npx vitest run
    ```
    Then start the dev server:
    ```bash
    cd gapstrike && npx next dev
    ```
    Confirm the dev server starts without errors on localhost:3000 (or the configured port).
  </action>
  <verify>
    <automated>cd gapstrike && npx vitest run</automated>
  </verify>
  <done>All tests pass and dev server is running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full flowchart and table editing integration in GapStrike:
    - FlowchartEditor with inline label editing, add/remove boxes, connect/disconnect nodes, reorder
    - TableEditor with cell/header editing, add/remove rows and columns
    - Both editors propagate changes to the card FRONT field via onChange
  </what-built>
  <how-to-verify>
    1. Open GapStrike in browser (localhost:3000 or configured port)
    2. Navigate to an existing note or create one with content

    **Test INTG-01 — Flowchart Editor:**
    3. Click the "Flowchart" button in the Anki card panel
    4. Wait for AI generation to complete
    5. Verify: FlowchartEditor opens showing boxes and arrows
    6. Click a box — verify a textarea appears for editing
    7. Change the text and click away (blur) — verify the box updates
    8. Click "Add Box" in the toolbar — verify a new box appears
    9. Hover a box and click the delete (x) button — verify the box is removed
    10. Click "Connect" in the toolbar, click one box, then another — verify an edge is created
    11. Click "Preview in Anki" — verify the HTML preview reflects all edits

    **Test INTG-02 — Table Editor:**
    12. Click the "Table" button in the Anki card panel
    13. Wait for AI generation to complete
    14. Verify: TableEditor opens showing a grid with headers and rows
    15. Click a cell and edit its content — verify the cell updates
    16. Click "+ Add row" — verify a new row appears
    17. Click "Preview in Anki" or check the raw HTML — verify edits are reflected
  </how-to-verify>
  <resume-signal>Type "approved" if both editors work end-to-end, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of INTG-01 and INTG-02 in the browser.
</verification>

<success_criteria>
- Flowchart button -> AI generation -> FlowchartEditor with working editing -> FRONT field updates
- Table button -> AI generation -> TableEditor with working editing -> FRONT field updates
- No console errors during the flow
- User approves both integration paths
</success_criteria>

<output>
After completion, create `.planning/phases/04-editing-operations/04-04-SUMMARY.md`
</output>
