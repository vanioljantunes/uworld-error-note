---
phase: 08-richer-ai-template-atomic
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - gapstrike/src/lib/template-defaults.ts
autonomous: false
requirements:
  - TMPL-07

must_haves:
  truths:
    - "A newly generated flowchart card from a real USMLE extraction contains 5-7 boxes with labeled causal arrows"
    - "Cloze syntax appears on mechanism steps (distinguishing steps), not on triggers or leaf outcomes"
    - "parseFlowHTML correctly parses the actual GPT-4o output without triggering textarea fallback"
    - "The rendered preview in FlowchartEditor looks correct visually"
  artifacts:
    - path: "gapstrike/src/lib/template-defaults.ts"
      provides: "Potentially iteration-tuned template (same file as 08-01)"
  key_links:
    - from: "gapstrike/src/lib/template-defaults.ts"
      to: "GPT-4o API"
      via: "Template prompt sent as system message for card generation"
      pattern: "anki_flowchart"
---

<objective>
Test the updated template against 5 diverse real USMLE extractions via live GPT-4o generation, verify all 5 produce parseable HTML with 5-7 nodes, and human-verify the rendered visual output in the editor preview.

Purpose: GPT-4o prompt compliance is not guaranteed — this plan validates the template works in practice and allows one iteration cycle if the model ignores constraints (e.g., produces 3-4 nodes, misplaces cloze).
Output: Confirmed working template with 5/5 successful parse results; visual verification of rendered flowcharts
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-richer-ai-template-atomic/08-CONTEXT.md
@.planning/phases/08-richer-ai-template-atomic/08-RESEARCH.md
@.planning/phases/08-richer-ai-template-atomic/08-01-SUMMARY.md

@gapstrike/src/lib/template-defaults.ts
@gapstrike/src/lib/parse-flow-html.ts
@gapstrike/tests/flow-round-trip.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 5 flowcharts from diverse USMLE extractions and validate parseability</name>
  <files>gapstrike/src/lib/template-defaults.ts</files>
  <action>
    Create a temporary test script (or use the app's generation flow) to generate flowcharts from 5 diverse USMLE-style extractions. The extractions should cover different domains to stress-test the template:

    1. **Pathophysiology**: e.g., heart failure mechanism (preload/afterload chain)
    2. **Pharmacology**: e.g., beta-blocker mechanism or statin pathway
    3. **Infectious disease**: e.g., TB granuloma formation
    4. **Renal**: e.g., nephrotic syndrome mechanism
    5. **Neurology**: e.g., stroke pathway or myasthenia gravis

    For each extraction, compose a realistic text block with educational objective and wrong alternatives (matching what the app sends to GPT-4o).

    Use the app's existing generation endpoint (`/api/create-card` or equivalent) to send each extraction with the updated `anki_flowchart` template. Capture the returned HTML.

    For each of the 5 generated HTML outputs:
    1. Feed into `parseFlowHTML()` — verify it does NOT throw or return a structure that would trigger `parseFailed`
    2. Count nodes — verify >= 5 nodes
    3. Check cloze placement — verify cloze appears on intermediate mechanism steps, not on the first (trigger) or last (leaf outcome) boxes
    4. Check arrow labels — verify they use domain-specific verbs (not "leads to", "causes", "then")

    **If any output fails** (< 5 nodes, parse failure, cloze on trigger/leaf):
    - Diagnose which template instruction GPT-4o ignored
    - Tweak the template wording in `template-defaults.ts` (e.g., strengthen the 5-7 node rule, add "IMPORTANT:" prefix, adjust example emphasis)
    - Re-test that specific extraction
    - Maximum 2 iteration cycles — if still failing after 2 tweaks, document the failure pattern and proceed to human-verify

    After all 5 pass, run the full test suite to confirm no regressions from any template tweaks.
  </action>
  <verify>
    <automated>cd gapstrike && npx vitest run</automated>
  </verify>
  <done>5/5 generated flowcharts produce parseable HTML with >= 5 nodes each, cloze on mechanism steps, domain-specific arrow labels. All vitest tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify richer flowchart generation in editor</name>
  <action>Richer AI flowchart template producing 5-7 node mechanism maps with educational-objective-driven cloze and labeled causal arrows. Template atomically updated with parser compatibility and hash-based auto-upgrade.</action>
  <how-to-verify>
    1. Start the dev server: `cd gapstrike && npm run dev`
    2. Open the app in the browser
    3. Navigate to the Anki card creation flow
    4. Generate a flowchart card from any USMLE extraction text
    5. Verify the generated flowchart has:
       - 5-7 boxes (not the old 3-4 box shallow chains)
       - Arrow labels with specific medical verbs (e.g., "inhibits", "activates", "depletes") — not generic "leads to" or "causes"
       - Cloze syntax on mechanism steps (middle boxes), not on the trigger (first box) or leaf outcomes (last boxes)
       - Category hints on clozed terms (e.g., Thiamine::vitamin format)
    6. Click "Preview" mode — verify the flowchart renders correctly (boxes, arrows, labels all visible)
    7. Click "Edit" mode — verify you can still edit box labels and the HTML updates
    8. Optionally push the card to Anki and verify it renders correctly there
  </how-to-verify>
  <verify>Human confirms visual output matches expectations</verify>
  <done>Human approves that generated flowcharts have 5-7 nodes, domain-specific arrow labels, cloze on mechanism steps, and render correctly in both Preview and Edit modes.</done>
  <resume-signal>Type "approved" or describe any issues with the generated flowcharts</resume-signal>
</task>

</tasks>

<verification>
1. 5 diverse USMLE extractions all produce parseable 5-7 node flowcharts
2. No parse failures (no textarea fallback triggered)
3. Cloze placement is on distinguishing mechanism steps, not triggers/leaves
4. Arrow labels use domain-specific vocabulary
5. Human confirms visual output looks correct in Preview and Edit modes
6. Full vitest suite passes
</verification>

<success_criteria>
- 5/5 generated flowcharts have >= 5 nodes with labeled causal arrows
- parseFlowHTML handles all 5 outputs without fallback
- Cloze appears on mechanism steps only (validated on all 5)
- Human approves visual rendering in editor Preview mode
- All tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/08-richer-ai-template-atomic/08-02-SUMMARY.md`
</output>
