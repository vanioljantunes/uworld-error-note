---
phase: 05-polish-and-deploy
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: []
autonomous: false
requirements: [TMPL-06]
must_haves:
  truths:
    - "A flowchart card generated in GapStrike renders on AnkiDroid with boxes visible and text readable"
    - "A table card generated in GapStrike renders on AnkiDroid with rows/columns visible"
    - "Cloze syntax reveals and hides correctly on tap in AnkiDroid"
    - "No critical layout breakage (boxes overlapping, text invisible, arrows missing)"
  artifacts: []
  key_links: []
---

<objective>
Verify that flowchart and table cards render correctly on AnkiDroid. Sync test cards from Anki desktop to AnkiDroid and visually confirm boxes, arrows, cloze reveal/hide, and table layout.

Purpose: TMPL-06 requires generated HTML to render correctly on mobile. This manual checkpoint confirms the inline-style HTML works on AnkiDroid's WebView.
Output: Visual confirmation or documented issues requiring CSS fixes.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-deploy/05-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: AnkiDroid rendering smoke-test for flowchart and table cards</name>
  <files></files>
  <action>
Human verifies that flowchart and table cards render correctly on AnkiDroid.

Steps:
1. Open GapStrike locally (npm run dev)
2. Generate a flowchart card: use an existing extraction, click "Flowchart", wait for AI generation, optionally edit a box label, then save to Anki
3. Generate a table card: use an existing extraction, click "Table", wait for AI generation, then save to Anki
4. Open Anki desktop -- confirm both cards appear in the deck
5. Sync Anki desktop to AnkiConnect cloud (or use direct AnkiDroid sync if configured)
6. Open AnkiDroid on your phone/tablet/emulator
7. Navigate to the deck and review the flowchart card:
   - Are boxes visible with text?
   - Do arrows/stems appear between boxes?
   - Do step labels (pills) appear?
   - Tap a cloze -- does it reveal the hidden text?
8. Review the table card:
   - Are rows and columns visible?
   - Is text readable in cells?
   - Tap a cloze -- does it reveal correctly?

Acceptance bar (per user decision): Boxes visible with text, arrows/labels appear, cloze reveals/hides correctly on tap. NOT pixel-perfect -- layout differences between desktop and mobile are acceptable.

If issues found: Document which CSS properties break (e.g., display:inline-flex on branch wrappers, rgba() border colors). These will be fixed before deploy in a gap-closure plan.

Resume signal: Type "approved" if both card types render acceptably, or describe specific rendering issues.
  </action>
  <verify>
    <automated>MANUAL — requires physical device or emulator for AnkiDroid rendering verification</automated>
  </verify>
  <done>Both flowchart and table cards render acceptably on AnkiDroid: boxes visible, text readable, cloze tap reveals content, no critical layout breakage.</done>
</task>

</tasks>

<verification>
Human visual confirmation that both flowchart and table cards render on AnkiDroid.
</verification>

<success_criteria>
- Flowchart card: boxes visible, text readable, cloze tap works
- Table card: rows/columns visible, text readable, cloze tap works
- No critical layout breakage documented
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-deploy/05-02-SUMMARY.md`
</output>
