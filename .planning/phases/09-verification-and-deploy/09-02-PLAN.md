---
phase: "09-verification-and-deploy"
plan: 2
type: "execute"
wave: 2
depends_on: ["09-01"]
files_modified:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
autonomous: false
requirements: []
must_haves:
  truths:
    - "gapstrike.vercel.app shows the two-mode UI (Preview/Edit tabs) confirming v1.1 code is deployed"
    - "A flowchart card generated on production has 5-7 nodes with domain-specific arrows"
    - "REQUIREMENTS.md marks all v1.1 requirements as Complete"
    - "ROADMAP.md marks Phase 9 as Complete and v1.1 milestone as shipped"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.1 requirement completion status"
      contains: "[x] **UX-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 and v1.1 milestone completion"
      contains: "Complete"
    - path: ".planning/STATE.md"
      provides: "Updated project state"
      contains: "Phase 9"
  key_links:
    - from: "git push origin master"
      to: "gapstrike.vercel.app"
      via: "Vercel auto-deploy"
      pattern: "Preview.*Edit"
---

<objective>
Push all 45 unpushed v1.1 commits to origin/master to trigger Vercel auto-deploy, then smoke-test the production app at gapstrike.vercel.app to confirm all v1.1 changes are live. Finally, update planning docs to mark v1.1 as shipped.

Purpose: Ship v1.1 to production and close the milestone with accurate documentation.
Output: Live production deployment at gapstrike.vercel.app with all v1.1 features, updated REQUIREMENTS.md / ROADMAP.md / STATE.md.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-verification-and-deploy/09-01-SUMMARY.md
@.planning/phases/09-verification-and-deploy/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to Vercel and update planning docs</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Step 1: Push all commits to origin/master to trigger Vercel auto-deploy.
```bash
git push origin master
```
Vercel auto-deploys from master. No Vercel CLI needed. Build takes 60-120 seconds.

Step 2: Update REQUIREMENTS.md — mark all v1.1 requirements as complete:
- Change `[ ] **UX-01**` to `[x] **UX-01**`
- Change `[ ] **UX-02**` to `[x] **UX-02**`
- Change `[ ] **BUG-01**` to `[x] **BUG-01**`
- Change `[ ] **BUG-02**` to `[x] **BUG-02**`
- Update the traceability table: set UX-01, UX-02, BUG-01, BUG-02 status to "Complete"
- Update coverage line: "v1.1 requirements: 8 total — all Complete"

Step 3: Update ROADMAP.md:
- Phase 9 plans line: "Plans: 2 plans"
- Phase 9 plan list: checkmark both 09-01 and 09-02
- Phase 9 status in progress table: "Complete" with date
- v1.1 milestone header: change from "In Progress" to shipped

Step 4: Update STATE.md:
- status: milestone-complete
- stopped_at: "v1.1 shipped — all 9 phases complete, deployed to gapstrike.vercel.app"
- progress percent: 100% (9/9 phases)
- completed_plans: updated total including Phase 9 plans

Do NOT commit these doc changes yet — wait for human verification of the deploy in Task 2. The docs will be committed after production confirmation.
  </action>
  <verify>
    <automated>git log origin/master --oneline -1</automated>
  </verify>
  <done>All commits pushed to origin/master. REQUIREMENTS.md, ROADMAP.md, STATE.md updated locally (pending commit after production verification).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production smoke-test at gapstrike.vercel.app</name>
  <action>All 45 v1.1 commits pushed to origin/master, triggering Vercel auto-deploy. Planning docs updated to reflect v1.1 completion (pending commit after this verification). Wait 60-120 seconds for Vercel build to complete before testing.</action>
  <how-to-verify>
**Wait for Vercel deploy to complete** (~60-120 seconds after push). Check https://vercel.com dashboard if needed.

**Production smoke-test (5 min):**

1. Open https://gapstrike.vercel.app in browser (hard refresh with Ctrl+Shift+R)
2. VERIFY: The app loads without errors

**Two-mode UI check (confirms SC-4):**
3. Navigate to a card with an extraction, click "Flowchart"
4. VERIFY: Editor shows **Preview/Edit tab pair** (NOT old "Preview in Anki" / "Back to Editor" toggle)
5. VERIFY: No eye-toggle visible in flowchart/table mode

**Flowchart generation check (confirms SC-2):**
6. VERIFY: Generated flowchart has **5-7 boxes** with domain-specific arrow labels
7. Click "Push to Anki" — VERIFY card reaches Anki (if Anki desktop is available; skip if not running on deploy machine)

**Table check (confirms SC-3):**
8. Click "Table" format button on a card with an extraction
9. VERIFY: Table renders, cell editing works
10. Click "Push to Anki" — VERIFY success (if Anki available)

**Warning signs of failed deploy:**
- Old toggle labels ("Preview in Anki" / "Back to Editor") instead of Preview/Edit tabs
- 3-4 node flowcharts instead of 5-7 nodes
- If old UI appears: check Vercel dashboard for build status, trigger manual redeploy if needed
  </how-to-verify>
  <verify>Human confirms production app at gapstrike.vercel.app reflects all v1.1 changes</verify>
  <done>Production deployment verified: gapstrike.vercel.app shows two-mode UI, generates 5-7 node flowcharts, table editing works. v1.1 milestone is shipped.</done>
  <resume-signal>Type "approved" if production app reflects all v1.1 changes, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- git push to origin/master succeeded
- Vercel deploy completed successfully
- Production app at gapstrike.vercel.app shows two-mode UI (Preview/Edit)
- Production flowchart generates 5-7 nodes with domain-specific arrows
- Production table editor works (generate, edit, push)
- REQUIREMENTS.md marks all v1.1 requirements Complete
- ROADMAP.md marks Phase 9 Complete and v1.1 shipped
- STATE.md reflects milestone completion
</verification>

<success_criteria>
v1.1 Editor Polish milestone is shipped: production app at gapstrike.vercel.app reflects all changes from Phases 6-8, planning documentation is updated, and the milestone is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-and-deploy/09-02-SUMMARY.md`
</output>
