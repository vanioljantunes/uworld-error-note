---
phase: "09-verification-and-deploy"
plan: 1
type: "execute"
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 94+ Vitest tests pass with zero failures"
    - "npm run build completes with zero TypeScript errors"
    - "Flowchart card generates with 5-7 nodes and domain-specific arrows in local dev"
    - "Flowchart card pushes to Anki successfully via AnkiConnect"
    - "Table card generates, cell editing works, and pushes to Anki successfully"
    - "Preview/Edit two-mode UI is visible (not old three-mode toggle)"
    - "Escape aborts edge creation without phantom edges (BUG-03)"
    - "Back field shows extraction text after flowchart generation (BUG-04)"
  artifacts: []
  key_links:
    - from: "gapstrike/vitest.config.ts"
      to: "gapstrike/tests/**"
      via: "npx vitest run"
      pattern: "94 tests.*pass"
    - from: "gapstrike/package.json"
      to: "gapstrike/.next/"
      via: "npm run build"
      pattern: "Compiled successfully"
---

<objective>
Run the full automated test suite and build gate, then smoke-test flowchart and table end-to-end on the local dev server to confirm all v1.1 changes (Phases 6-8) work correctly before deploying.

Purpose: Gate deployment — no code reaches production until local verification passes. This catches TypeScript errors, test regressions, and the OneDrive stale-bundle trap before pushing 45 commits to Vercel.
Output: Green test suite, successful build, human-verified local smoke-test confirming all v1.1 features work.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-verification-and-deploy/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite and build gate</name>
  <files></files>
  <action>
Run the full Vitest suite and npm build gate in sequence. No code changes expected — this is pure verification.

Step 1: Run Vitest
```bash
cd gapstrike && npx vitest run
```
Expect: 94+ tests pass, 0 failures, 0 skipped across 9 test files.

Step 2: Clear .next cache and run production build
```bash
rm -rf gapstrike/.next
cd gapstrike && npm run build
```
Expect: "Compiled successfully" with zero TypeScript errors and zero build warnings from v1.1 changes.

If any test fails or build errors occur: diagnose and fix the issue before proceeding. Do NOT skip failures — they are blockers for deployment.

CRITICAL: The .next/ cache MUST be cleared before the build because OneDrive + Windows FSWatcher can cause stale bundles (confirmed in Phase 6 UAT).
  </action>
  <verify>
    <automated>cd gapstrike && npx vitest run && rm -rf .next && npm run build</automated>
  </verify>
  <done>All Vitest tests pass (0 failures). npm run build completes with zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Local smoke-test — flowchart and table end-to-end</name>
  <action>All v1.1 changes from Phases 6-8 (two-mode UI, bug fixes, richer AI template) running on local dev server. The automated test suite and build gate have passed. Before this checkpoint, start a fresh dev server: rm -rf gapstrike/.next and cd gapstrike and npm run dev. Wait for "Ready" message before proceeding.</action>
  <how-to-verify>
**Prerequisites:** Anki desktop must be open with AnkiConnect add-on installed (port 8765). Verify with: curl http://localhost:8765

**Flowchart smoke-test (5 min):**
1. Open http://localhost:3000 in browser
2. Select a card with an extraction (or create one)
3. Click "Flowchart" format button
4. VERIFY: Editor opens in **Preview mode** (rendered Anki card, NOT raw node/edge editing UI) — confirms UX-01
5. VERIFY: Only **Preview** and **Edit** tabs visible (no other view mode buttons) — confirms UX-02
6. VERIFY: Flowchart shows **5-7 boxes** with domain-specific arrow labels (e.g., "inhibits", "activates", not "leads to") — confirms TMPL-07
7. VERIFY: **Back field** shows original extraction text, NOT generated HTML — confirms BUG-04
8. Click **Edit** tab — confirm raw node/edge editing UI appears
9. In Edit mode: click Connect, then press **Escape** — VERIFY no phantom edge is created — confirms BUG-03
10. In Edit mode: try removing a node — VERIFY no crash, edges reconnect correctly
11. Click **Preview** tab — confirm returns to rendered view
12. Click "Push to Anki" — VERIFY AnkiConnect returns success, card appears in Anki desktop
13. Check browser console — VERIFY no errors throughout

**Table smoke-test (3 min):**
1. Select a card with an extraction
2. Click "Table" format button
3. VERIFY: Table editor renders correctly
4. Click a cell — VERIFY inline editing works (type new text, see it update)
5. Click "Push to Anki" — VERIFY AnkiConnect returns success, card appears in Anki
6. Check browser console — VERIFY no errors

**Layout check (1 min):**
7. Resize browser window to narrow width (~400px)
8. VERIFY: Format button row (Flowchart, Table, Cloze) wraps to second line instead of overflowing — confirms LAY-01
  </how-to-verify>
  <verify>Human confirms all smoke-test checks pass for flowchart, table, and layout</verify>
  <done>Local smoke-test approved: flowchart generates 5-7 nodes with domain arrows, table editing works, both push to Anki, two-mode UI confirmed, BUG-03/BUG-04 verified, layout wraps correctly.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any failures with step numbers</resume-signal>
</task>

</tasks>

<verification>
- Vitest suite: 94+ tests, 0 failures
- npm run build: zero TypeScript errors
- Local smoke-test: flowchart and table end-to-end confirmed by human
- All v1.1 requirements exercised: UX-01, UX-02, LAY-01, BUG-01 (via edit-mode node removal), BUG-02, BUG-03, BUG-04, TMPL-07
</verification>

<success_criteria>
Local verification is complete: tests green, build clean, flowchart and table smoke-tested end-to-end on local dev server with all v1.1 features confirmed working. Ready for production deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-and-deploy/09-01-SUMMARY.md`
</output>
