---
phase: 05-polish-and-deploy
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified: []
autonomous: false
requirements: [TMPL-06]
must_haves:
  truths:
    - "Local next build passes with zero TypeScript errors"
    - "Git push triggers Vercel auto-deploy successfully"
    - "Production app at gapstrike.vercel.app loads without errors"
    - "Full flow works on production: generate flowchart card, edit, save to Anki"
    - "Full flow works on production: generate table card, edit, save to Anki"
  artifacts: []
  key_links:
    - from: "git push"
      to: "Vercel auto-deploy"
      via: "Vercel git integration"
      pattern: "vercel"
---

<objective>
Deploy the complete flowchart/table editor feature to production on Vercel. Run local build first to catch TypeScript errors, push to git, then verify the production app works end-to-end.

Purpose: Ship the v1 feature. All code changes (fallbacks, editors, templates) are complete. This is the final deploy step.
Output: Live production app with all flowchart and table editor features.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-deploy/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local build verification and git push</name>
  <files></files>
  <action>
1. Run `cd gapstrike && npm run build` to verify the production build passes with zero TypeScript errors.
2. If build fails: fix the TypeScript/build errors, then re-run build until it passes.
3. Run `cd gapstrike && npx vitest run` to confirm all tests still pass.
4. Once build and tests are green, push to the remote: `git push origin master`.
5. Wait for Vercel to detect the push and begin deployment (Vercel auto-deploys from git push; no manual deploy command needed).

Do NOT use `vercel` CLI -- the project uses git-based auto-deploy.
Do NOT skip the local build step -- TypeScript errors are fatal on Vercel (per user decision).
  </action>
  <verify>
    <automated>cd gapstrike && npm run build && npx vitest run</automated>
  </verify>
  <done>
- `npm run build` exits 0 with no TypeScript errors.
- All Vitest tests pass.
- `git push` completed successfully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production smoke-test on Vercel</name>
  <files></files>
  <action>
Human verifies the production deployment works end-to-end.

Steps:
1. Wait for Vercel deployment to complete (check Vercel dashboard or wait ~2 minutes after push)
2. Open https://gapstrike.vercel.app in your browser
3. Open browser DevTools console (F12)

Flowchart smoke test:
4. Select an extraction, click "Flowchart" to generate a flowchart card
5. Verify the visual editor opens with boxes and arrows
6. Edit a box label, confirm it updates
7. Save the card to Anki (confirm AnkiConnect push succeeds)
8. Check console -- no errors

Table smoke test:
9. Select an extraction, click "Table" to generate a table card
10. Verify the table editor opens with rows and columns
11. Edit a cell, confirm it updates
12. Save the card to Anki
13. Check console -- no errors

Acceptance bar: Both flows complete without console errors. Cards save successfully to Anki. No visual regressions compared to localhost.

Resume signal: Type "approved" if production smoke test passes, or describe issues found.
  </action>
  <verify>
    <automated>MANUAL — requires production environment verification at gapstrike.vercel.app</automated>
  </verify>
  <done>Production app loads, both flowchart and table generation/editing/save flows work without console errors.</done>
</task>

</tasks>

<verification>
1. `cd gapstrike && npm run build` -- zero errors
2. Production app loads and functions end-to-end
3. No console errors during full flow smoke test
</verification>

<success_criteria>
- Production build passes locally
- Vercel deployment succeeds
- Flowchart generation, editing, and save works on production
- Table generation, editing, and save works on production
- Zero console errors during smoke test
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-deploy/05-03-SUMMARY.md`
</output>
